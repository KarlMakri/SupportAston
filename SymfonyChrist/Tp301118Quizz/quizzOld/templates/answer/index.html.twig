{% extends 'base.html.twig' %}



{% block body %}

    <div class="container">

        <a href="{{ path('question_add') }}" class="btn btn-primary">Ajouter une Réponse à un Question</a>

        {% for answer in answers %}

            <article class="question">
                <header>
                    {#{% for topic in proverb.topic %}#}
                    {#{{topic.name}}&nbsp;&nbsp;#}
                    {#{% endfor %}#}
                </header>
                {{answer.correct}}
                <footer>
                    <table>
                        <tr>
                            {#<td>QuestionOld {{ question.category.name }}</td>#}
                            {#<td><a href="" class="btn btn-info btn-sm">Répondre</a></td>#}
                            {#<td><a href="{{ path('question_edit', {'id' : question.id}) }}" class="btn btn-warning btn-sm">Modifier</a></td>#}
                            {#<td><a href="{{ path('question_delete', {'id' : question.id}) }}" class="btn btn-danger btn-sm">Supprimer</a></td>#}
                        </tr>
                    </table>
                </footer>
            </article>

        {% endfor %}

    </div>


{% endblock %}
